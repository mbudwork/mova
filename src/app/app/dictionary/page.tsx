import { redirect } from 'next/navigation';

/**
 * Раздел снят с навигации: он дублировал курс и разбавлял главный сценарий.
 * Страница оставлена редиректом, а не удалена, — по адресу могли остаться
 * закладки и ссылки, и 404 на них выглядела бы поломкой. Рабочая версия
 * лежит в истории git и возвращается одним коммитом.
 */
export default function DictionaryPage() {
  redirect('/app');
}
